FROM mitodl/concourse-packer-resource
ENV PYTHONUNBUFFERED=1
CMD ["/bin/sh"]

RUN apk add --no-cache --update build-base && \
    pip install --no-cache-dir ansible pyinfra && \
    apk del build-base
